1) npm init --y
2) npm install express sequelize sequelize-cli mysql2
3) sequelize init (else in cmd npm install -g sequelize-cli )
4) create db in mysql 
5) provide db name, password in config.json 
6) create model 
7) sync db sequelize in server.js
8) create routes 
9) sequelize using db.User.create or User.create => const { User } = require('./models')